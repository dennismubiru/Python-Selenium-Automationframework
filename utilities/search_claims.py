class Search_claims_data:

    def __init__(self, employee_name, reference_ID, event,status, from_date, to_date, include):
        self.employee_name = employee_name
        self.reference_ID = reference_ID
        self.event = event
        self.status = status
        self.from_date = from_date
        self.check_out = to_date
        self.adults_num = include

